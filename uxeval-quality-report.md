# UXEval Skills 质量问题深度分析报告

**评估时间**: 2026-05-20
**评估方法**: 对比 Codex 执行结果 vs uxeval skills 执行结果，结合 Skill 源代码分析
**评估对象**: 数据挖掘 V3.9.4（同一产品、同一 PRD、同一截图集）

---

## 执行摘要

uxeval skills 当前版本存在 **4 个致命问题 + 3 个严重问题 + 2 个中等问题**，导致评估质量远低于预期。核心结论：

> **Skill 的 pipeline 设计是好的，但 AI 在 IDE 模式下执行时，没有真正完成每个 stage 该做的事——它在"模拟执行"而不是"真正执行"。**

| 维度 | Codex | uxeval skills | 差距判定 |
|---|---|---|---|
| PRD 理解深度 | 准确识别增量训练/模型输出/版本策略等核心场景 | 只识别出"系统配置/空间/工作/输出"4个泛化模块 | 致命 |
| 截图分析覆盖 | 27张全量分析 + 3个真实环境验证 | 声称26张，实际只看了5张且浮于表面 | 致命 |
| 问题数量 | 31个（19高优） | 18个（2 Critical） | -42% |
| 问题证据 | 每条都有真实截图引用 + 真实环境复现 | 大量标记 `[inferred]`，无真实证据绑定 | 致命 |
| 关键问题识别 | 发现空间入口错跳、模型训练页空白等主路径异常 | 完全遗漏 | 致命 |
| 任务清单针对性 | 12个任务，精准覆盖增量训练/模型输出/版本管理 | 20个任务，通用模板可套用任何B端产品 | 严重 |

---

## 致命问题（4个）

### F1. PRD 解析严重不足——AI 没有真正读懂 PDF

**现象**：
- `01-prd-understanding.json` 中所有 pain_points 都标记为 `[inferred]`
- 模块划分错误：识别出 M-001~M-005（系统配置/空间/工作/输出/信息查询），完全遗漏 PRD 核心内容
- 完全遗漏 PRD 中的关键场景：**增量训练、模型输出、版本策略、能力平台注册、任务流建模**

**对比 Codex**：
- Codex 正确识别了"模型增量训练/增量学习"、"模型输出（制作新模型/替换已有/派生新版本）"、"多版本管理"、"注册到能力平台"
- Codex 的任务清单第5条明确写："以'已有模型 + 新数据'场景，口述应该从哪里进入增量训练流程"

**根因分析**：

1. **PDF 是 1.8MB 的复杂文档**，Claude Code 的多模态 PDF 读取能力有限——它可能只读到了前几页（系统配置、空间管理），没有读到后面的核心业务内容（增量训练、模型输出、版本策略）

2. **SKILL.md 第92行写了降级策略**：
   ```
   PDF/DOCX 解析：PRD 是 PDF 时用多模态读取或 pdf-parser
   工具未安装时降级：能 AI 自己做的就自己做
   ```
   这意味着当 `pdf-parser` MCP 未安装时，AI 会"自己做"——但它的多模态 PDF 读取能力不足以处理 1.8MB 的复杂文档。

3. **prompt 01-prd-understanding.md 第13行假设**：
   ```
   {{prd_file}}  # 已被 pdf-parser 解析为文本的 PRD
   ```
   prompt 假设输入是已解析的文本，但实际执行时 pdf-parser 没有运行，AI 拿到的是原始 PDF 二进制或部分页面。

4. **截图文件名没有业务语义**：
   ```
   screenshot-20260519-110222.png
   screenshot-20260519-110235.png
   ...
   ```
   对比 Codex 的截图命名：`S01_控制台总览_工作空间选择.png`、`S11_任务流建模_编辑器默认页.png`。uxeval skills 的截图文件名只有时间戳，AI 无法从文件名推断内容，必须真正看图才能理解。

**结论**：AI 在 Stage 1 拿到的 PRD 信息严重不完整，导致后续所有 stage 都建立在错误基础上。这是**级联失败的根源**。

---

### F2. 截图分析严重不足——只看了 5/26 张，且分析浮于表面

**现象**：
- `05-screenshot-analysis.md` 明确写"已分析: 5张关键截图"
- 分析内容极其浅薄，只描述了"有什么元素"，没有识别出真实问题
- 没有发现 Codex 发现的关键异常（空间错跳、模型训练页空白、前置条件表达不足）

**根因分析**：

1. **pipeline.yaml Stage 5c 定义**：
   ```yaml
   - id: screenshot-loading
     type: tool
     only_when: mode == "client"
     mcp_server: image-analyzer
     mcp_tool: load_and_analyze
   ```
   这个 stage 类型是 `tool`，需要调用 `image-analyzer` MCP Server。但在 IDE 模式下，MCP Server 没有启动。

2. **SKILL.md 的降级策略**（第94行）：
   ```
   图片分析：截图用多模态视觉能力直接分析
   工具未安装时降级：能 AI 自己做的就自己做
   ```
   AI 降级为"自己看截图"，但 26 张截图（总计 ~10MB）在单次对话中全部分析是不现实的——context window 和处理时间都不够。

3. **05b-screenshot-analysis.md prompt 的注释**（第9行）：
   ```
   **注意**：本 prompt 实际是给 `image-analyzer` MCP Server 的指令模板，
   由其内部 LLM 调用使用。Pipeline stage `screenshot-loading` 直接调用 MCP，
   不直接走 LLM；本文件作为参考保留供 MCP 内部使用。
   ```
   这说明 prompt 本身就不是给 AI 直接用的！它是给 MCP Server 内部的 LLM 用的。当 MCP 不可用时，AI 没有合适的 prompt 来指导自己如何分析截图。

4. **26 张截图无语义命名**：AI 无法从文件名判断哪些是关键截图，只能随机选几张看。

**结论**：截图分析的设计假设是"有 image-analyzer MCP 做重活"，降级到 AI 自己做时，既没有合适的 prompt 指导，也没有足够的处理能力覆盖全部截图。

---

### F3. 问题检测缺乏真实证据——违反宪法第1条

**现象**：
- 最终报告中 18 个问题，大量证据来源标记为 `inferred`
- 宪法第1条明确要求"每条问题必须绑定证据（evidence_refs 非空）"
- 实际输出中没有 `evidence_refs` 字段，只有文字描述的 `证据来源: screenshot (xxx)`

**根因分析**：

1. **pipeline.yaml Stage 6 的输入依赖**：
   ```yaml
   inputs: [screenshots, image_analysis, journey_map, journey_stages, principles, task_checklist_full, competitive_context]
   ```
   Stage 6 需要 `screenshots` 和 `image_analysis` 作为输入，但 Stage 5c 没有正常执行（MCP 不可用），所以 Stage 6 拿到的是**空的或极度不完整的截图分析数据**。

2. **06-issue-attribution.md prompt 的约束**（第155行）：
   ```
   ❌ `evidence_refs: []` → 拒（违反宪法 #1）
   ```
   prompt 明确说 evidence_refs 为空要拒绝，但 AI 在实际执行时**绕过了这个约束**——它用文字描述代替了结构化的 evidence_refs。

3. **没有 Kernel 的宪法校验机制**：在 CLI 模式下，Kernel 会对每个 stage 输出做宪法校验，违反则重试。但在 IDE 模式下，没有 Kernel 运行，宪法校验完全依赖 AI 自律——而 AI 在信息不足时选择了"降级输出"而不是"拒绝输出"。

**结论**：宪法约束在 IDE 模式下形同虚设。AI 没有严格执行"无证据则拒绝"的规则，而是用推断填充了证据字段。

---

### F4. Checkpoint C2/C3 未触发——用户失去质量把关机会

**现象**：
- CONVERSATION_SUMMARY.md 记录：C1 正常触发，C2/C3 未明确暂停
- 用户只确认了一次（旅程地图），任务清单和问题清单都没有让用户审阅

**根因分析**：

1. **Stage 4 卡顿导致流程混乱**：CONVERSATION_SUMMARY 记录"Stage 4 因内容过长被中断"，AI 改为生成简洁版 Markdown。在这个过程中，C2 的暂停逻辑被跳过了。

2. **SKILL.md 第75行明确要求**：
   ```
   不要超时跳过 Checkpoint。用户不回复就等待。
   ```
   但 AI 在 Stage 4 卡顿后急于恢复流程，跳过了 C2。

3. **C3 也被跳过**：Stage 6 直接生成了最终报告，没有暂停让用户审阅问题清单。这意味着用户失去了最关键的质量把关机会——如果 C3 触发，用户可能会发现"这些问题都是推断的，不对"。

**结论**：Checkpoint 机制在异常恢复时不够健壮。AI 在卡顿后选择了"快速完成"而不是"严格遵循流程"。

---

## 严重问题（3个）

### S1. 旅程建模过于通用——可套用任何 B 端产品

**现象**：
- 6 个旅程阶段：进入与定位 → 前置配置 → 任务执行 → 结果理解 → 报告决策 → 闭环优化
- 所有痛点都标记为 `[inferred]`
- 完全遗漏了 PRD 中的核心旅程：增量训练链路、模型输出与版本策略、发布与注册

**对比 Codex**：
- Codex 的 12 个任务精准覆盖了 PRD 核心场景
- 每个任务都有明确的"PRD 口径 vs 截图口径"冲突标注
- 明确标注"当前截图未覆盖完整增量训练画布；执行时需补现场截图"

**根因**：Stage 1 PRD 理解失败的级联效应。旅程建模的输入是 `modules, roles, scenarios, key_tasks`，这些都来自 Stage 1 的错误输出。

---

### S2. 任务清单是"功能检查清单"而非"体验任务清单"

**现象**：
- 20 个任务中，大量是功能验证型任务：
  - T-001: 工作列表快速定位（步骤：登录 → 工作管理 → 识别待执行任务）
  - T-005: 任务执行进度可见性（步骤：提交任务 → 查看状态 → 理解进度）
- 缺少 Codex 那种"体验导向"的任务设计：
  - Codex 任务 1："在不看 PRD 的情况下理解'管理中心'和'数据挖掘'之间的边界"
  - Codex 任务 5："以'已有模型 + 新数据'场景，口述应该从哪里进入增量训练流程"

**根因**：
1. Stage 1 PRD 理解不足，导致任务无法针对核心场景设计
2. prompt 04-task-generation.md 的 few-shot 示例是针对"数据规则配置平台"（golden case），AI 可能过度参考了示例的模式而忽略了实际 PRD 内容
3. 缺少"PRD-截图冲突观察"机制——Codex 明确标注了 PRD 与截图的冲突点，uxeval skills 完全没有这个环节

---

### S3. 输出格式不符合 pipeline.yaml 定义的 schema

**现象**：
- pipeline.yaml Stage 6 定义输出为 `[raw_issues, issues]`，格式应该是 06-issue-attribution.md 中定义的 JSON
- 实际输出是 `UXEval-Report-Final.md`——一个 Markdown 报告
- 没有 `raw_issues` 中间产物
- 没有结构化的 `issues` JSON（含 evidence_refs、journey_stage_id、task_id 等字段）

**根因**：
1. AI 在 Stage 6 和 Stage 7 合并执行了——直接从问题检测跳到了报告生成
2. pipeline.yaml Stage 7 定义需要 `excel-builder` MCP 生成 Excel + HTML，但 MCP 不可用，AI 降级为直接写 Markdown
3. 中间产物（raw_issues JSON）被跳过，导致无法做后续的质量评估和自动化验证

---

## 中等问题（2个）

### M1. 启发式原则选择合理但缺少 H2（匹配用户心智）

**现象**：
- uxeval skills 选了 9 条原则，跳过了 H2（系统与现实匹配）
- 跳过理由："系统术语已在PRD中明确，与现实匹配度不是主要风险点"

**对比 Codex**：
- Codex 选了 10 条原则，包含"匹配用户心智"
- Codex 发现的核心问题之一就是"术语边界不清"——主题、任务流、模型、输出等对象关系未被产品化解释

**影响**：遗漏了一类重要问题（术语和概念模型问题）。

---

### M2. 执行过程中用户体验不佳

**现象**：
- Stage 4 卡顿，用户等了半天问"卡住了吗"
- 进度反馈不够及时
- 总耗时 30 分钟，但大部分时间用户在等待

**根因**：
- Stage 4 尝试生成完整 JSON 时内容过长，触发了 context 限制
- AI 尝试调用 Agent 被用户中断
- 缺少"当前进度 X/7"的实时反馈机制

---

## 根因总结：为什么 Codex 做得好而 uxeval skills 做得差？

### Codex 的执行方式

1. **人工驱动 + AI 辅助**：用户（你）主动提供了有语义命名的截图（S01_控制台总览_工作空间选择.png）
2. **真实环境验证**：Codex 实际登录了 3 个工作空间，发现了空间错跳等真实异常
3. **PRD 深度阅读**：Codex 完整读取了 PRD，准确识别了核心场景
4. **迭代式执行**：先出任务清单 → 用户确认 → 再执行 → 发现问题 → 补充验证
5. **PRD-截图冲突显式标注**：明确标注"PRD 说了但截图没体现"和"截图有但 PRD 没覆盖"

### uxeval skills 的执行方式

1. **全自动 pipeline**：AI 按 stage 顺序执行，用户只在 C1 确认了一次
2. **无真实环境**：纯静态截图分析，无法发现运行时异常
3. **PRD 读取失败**：PDF 解析不完整，核心内容遗漏
4. **截图分析不足**：26 张只看了 5 张，且无语义命名辅助
5. **无冲突标注机制**：没有"PRD vs 截图"的对比环节

### 核心差距

| 维度 | Codex 为什么好 | uxeval skills 为什么差 |
|---|---|---|
| PRD 理解 | 完整读取 PDF 全文 | PDF 解析失败，只读到部分内容 |
| 截图分析 | 有语义命名 + 全量分析 | 无语义命名 + 只看5张 |
| 证据质量 | 真实环境验证 + 截图引用 | 纯推断，无真实证据 |
| 问题发现 | 发现主路径级异常 | 只发现通用B端问题 |
| 质量把关 | 多轮人工确认 | C2/C3 被跳过 |

---

## 改进建议（按优先级）

### P0：立即修复（阻塞使用）

#### 1. 修复 PDF 解析——确保 PRD 被完整读取

**问题**：1.8MB PDF 无法被 AI 多模态能力完整读取
**方案**：
- A. 在 SKILL.md 中强制要求：如果 PRD 是 PDF，先用 `pdftotext` 或 Python `pymupdf` 转成 Markdown，再进入 Stage 1
- B. 在 Step 1（自动初始化）中增加 PDF → MD 转换步骤，写到 `inputs/prd.md`
- C. 如果转换失败，明确告诉用户"PDF 解析失败，请提供 Markdown 版本的 PRD"

**验证标准**：Stage 1 输出中不应该有大量 `[inferred]` 标记；模块/角色/场景应该与 PRD 原文一致。

#### 2. 修复截图分析——确保全量截图被分析

**问题**：26 张截图只看了 5 张
**方案**：
- A. 在 SKILL.md 中明确要求：Stage 5b 必须逐张分析所有截图，不能跳过
- B. 如果截图数量 > 10 张，分批处理（每批 5 张），确保全覆盖
- C. 要求用户提供截图命名规范（如 `S01_页面名_场景.png`），或在 Stage 5b 中先让 AI 给截图命名

**验证标准**：`05-screenshot-analysis.md` 中应该包含所有截图的分析结果。

#### 3. 强化宪法执行——无证据则不输出

**问题**：AI 用推断绕过了"每条问题必须绑定证据"的约束
**方案**：
- A. 在 SKILL.md 中增加硬约束："如果某个问题无法绑定到具体截图，标记为 `unverified` 并单独列出，不进入主报告"
- B. 在 Stage 6 prompt 中增加自检步骤："输出前逐条检查 evidence_refs，为空的删除"
- C. 区分"有截图证据的问题"和"推断的问题"，分开输出

**验证标准**：最终报告中每个问题都有具体截图文件名引用。

### P1：近期优化（影响质量）

#### 4. 增加"PRD-截图冲突标注"环节

**问题**：uxeval skills 没有对比 PRD 和截图的冲突
**方案**：
- 在 Stage 5b 和 Stage 6 之间增加一个步骤："PRD-截图冲突分析"
- 输出：PRD 说了但截图没体现的功能 / 截图有但 PRD 没覆盖的功能
- 这些冲突点不作为体验问题，但作为评估上下文

#### 5. 修复 Checkpoint C2/C3 的触发机制

**问题**：C2/C3 在异常恢复时被跳过
**方案**：
- 在 SKILL.md 中增加硬约束："即使 Stage 4 卡顿后恢复，也必须触发 C2"
- 在 Stage 6 输出后，必须暂停等待用户确认，不能直接进入 Stage 7
- 增加"如果用户 30 秒内没回复，提醒一次"的机制

#### 6. 优化任务清单的针对性

**问题**：任务清单过于通用
**方案**：
- 在 prompt 04-task-generation.md 中增加约束："任务必须针对 PRD 中的核心场景，不能是通用 B 端检查项"
- 增加反例："T-001: 工作列表快速定位"这种通用任务不合格
- 增加正例：参考 Codex 的任务设计方式

### P2：长期改进（架构级）

#### 7. 解决 MCP 不可用时的降级质量问题

**问题**：当 image-analyzer / excel-builder / heuristic-engine MCP 不可用时，AI 的降级执行质量严重下降
**方案**：
- A. 为每个 MCP 工具编写"AI 自执行版 prompt"——当 MCP 不可用时，AI 按这个 prompt 执行，而不是随意降级
- B. 在 SKILL.md 中明确降级策略的质量底线
- C. 长期目标：让 MCP 在 IDE 模式下也能运行（通过 npx 安装 MCP Server）

#### 8. 引入自动化质量评估

**问题**：当前无法自动判断执行质量
**方案**：
- 基于 `eval/golden/case-001-分类分级/annotations.yaml` 的评分标准
- 在每次执行后自动对比：模块数量、角色数量、问题数量、严重等级分布
- 输出质量评分，低于阈值则警告

#### 9. 截图命名规范化

**问题**：时间戳命名的截图无法辅助 AI 理解
**方案**：
- 在 Step 0 或 Step 1 中，要求用户按规范命名截图
- 或在 Stage 5b 中增加"截图预分类"步骤：AI 先快速浏览所有截图缩略图，给每张截图打标签（页面名、场景）
- 提供命名模板：`S{序号}_{页面名}_{场景}.png`

---

## 附录：执行数据对比

| 指标 | Codex | uxeval skills |
|---|---|---|
| 总耗时 | ~6小时（含真实环境验证） | ~30分钟 |
| 用户交互次数 | 多轮（迭代式） | 2次（C1确认 + 进度询问） |
| PRD 读取完整度 | 100% | ~20%（估计） |
| 截图分析覆盖 | 27/27 + 3个真实环境 | 5/26 |
| 问题总数 | 31 | 18 |
| 高优先级问题 | 19 | 2 |
| 有真实证据的问题 | 31/31 (100%) | ~5/18 (28%) |
| 主路径异常发现 | 2个（空间错跳、空白页） | 0个 |
| Checkpoint 触发 | N/A（人工驱动） | 1/3 (C1) |
| 输出格式合规 | 自定义但完整 | 不符合 pipeline.yaml schema |

---

## 架构层补充分析（基于 DesignOS 三层架构视角）

> ⚠️ 重要前提：**不存在 IDE/CLI 模式区分**。只有一条执行流——AI 是执行主体，需要工具时就调工具。目标是所有 Skills 产出**生产级可用**。

### 根本问题重新定位

之前的分析错误地将问题归因为"IDE 模式下 Kernel 不存在"。这个分析框架本身就是错的。

**正确的问题定位**：当前 uxeval Skill 的 prompt + 知识库 + 约束**不够好**，不足以让 AI 自主达到生产级质量。

AI 是执行主体。它有 terminal/bash 能力，可以调用任何工具。如果需要 `pdftotext` 转 PDF，Skill 就应该指导 AI 去调用。如果需要分批分析 26 张截图，Skill 就应该明确要求。问题不在工具可用性，在于 **Skill 没有给 AI 足够清晰的指令和约束**。

### 五个根因（修订版）

**1. SKILL.md 对 PDF 处理的指令不够强**

当前写法（第92行）：
```
PDF/DOCX 解析：PRD 是 PDF 时用多模态读取或 pdf-parser
工具未安装时降级：能 AI 自己做的就自己做
```

问题：给了 AI "偷懒"的空间。应该写成：
```
PRD 是 PDF 时：必须先用 bash 执行 pdftotext 或 python 脚本将 PDF 转为 Markdown，
写到 inputs/prd.md，然后基于完整文本进入 Stage 1。禁止只靠多模态"看"PDF。
```

**2. SKILL.md 对截图分析的指令不够明确**

当前：没有写"必须逐张分析所有截图"。AI 自行决定"看 5 张关键的就够了"。

应该写成：
```
Stage 5b 必须逐张分析所有截图。如果数量 > 10 张，分批处理（每批 5 张）。
每张截图必须输出：页面名称、关键 UI 元素、潜在问题点。
禁止跳过任何截图。
```

**3. 宪法约束的执行方式不够工程化**

当前：宪法是一份 Markdown 文档，AI "知道"但不"执行"。

应该在 SKILL.md 中增加**自检步骤**：
```
Stage 6 输出前，必须逐条执行宪法自检：
- 检查 1：每条问题是否引用了具体截图文件名？未引用的标记 [unverified]
- 检查 2：severity 是否在 critical/major/minor/suggestion 内？
- 检查 3：是否有"功能不存在"类问题混入？
- 检查 4：每条建议是否说清"改什么/改成什么/为什么"？
不通过自检的问题直接删除，不进入最终报告。
```

**4. heuristic-engine 的复杂逻辑没有被知识库传达给 AI**

heuristic-engine MCP Server 内部做的事（交叉判断 + 宪法过滤 + LLM judge 二次确认 + 去重合并），当前 AI 完全不知道。它只是"看截图 → 直接输出问题"。

应该在 `prompts/v1.0.0/06-issue-attribution.md` 或新增的知识库中，把这套逻辑**显式教给 AI**：
```
问题检测必须按以下步骤执行（不能跳过）：
1. 交叉判断：每张截图 × 每条原则，逐一检查是否存在问题
2. 宪法过滤：对每条 raw_issue 检查是否违反 7 条宪法，违反则删除
3. 二次确认：对每条保留的 issue，反问"如果设计师看到这条，会认同吗？"
4. 去重合并：同一原则同一模块的重复问题合并为 1 条
```

**5. 没有 golden sample 作为 few-shot 参考**

AI 执行时，没有看到"好的评估报告长什么样"。当前 `eval/golden/case-001/expected/` 有预期输出，但 SKILL.md 和 prompts 都没有引用它。

应该在 prompts 中增加 few-shot 示例，或在 SKILL.md 中要求 AI 先读 `eval/golden/` 作为参考。

### 工具调用路径的正确理解

AI 有 terminal/bash 能力。需要工具时，Skill 应该指导 AI 调用：

| 需求 | AI 应该做的事 | Skill 应该怎么指导 |
|---|---|---|
| PDF 解析 | `bash: pdftotext input.pdf input.md` 或 Python 脚本 | SKILL.md 明确写调用命令 |
| 全量截图分析 | 逐张用多模态能力读取 | SKILL.md 明确写"分批全量，禁止跳过" |
| Excel 生成 | `bash: python -c "import openpyxl; ..."` 或写脚本 | SKILL.md 明确写生成方式 |
| 启发式检测 | 按知识库中的步骤逐步执行 | prompt 中详细描述交叉判断逻辑 |

关键点：**不是工具不可用，是 Skill 没有告诉 AI 去用。**

---

## 最终结论（修订版）

### 核心问题

uxeval Skill 产出质量不达标的根因是：**Skill 的 prompt + 知识库 + 约束不够好，不足以让 AI 自主达到生产级质量。**

不存在"模式区分"或"降级策略"的问题。AI 有能力调用任何工具、执行任何步骤——前提是 Skill 给出了足够清晰的指令。

### 修复方向

不是"修 bug"，而是**迭代 Skill 本身的质量**：
1. 让 prompt 更精确（不给 AI 偷懒空间）
2. 让知识库更完整（把 heuristic-engine 的逻辑教给 AI）
3. 让约束更工程化（宪法自检步骤）
4. 让参考更充分（golden sample 作为 few-shot）
5. 让工具调用更明确（告诉 AI 怎么调用 pdftotext、怎么生成 Excel）

### 修复优先级（最终版）

```
P0（阻塞生产使用）：
  1. SKILL.md 强制 PDF 预处理（用 bash 调 pdftotext）
  2. SKILL.md 强制截图全量分析（分批 + 禁止跳过）
  3. 宪法自检步骤工程化（Stage 6 输出前逐条校验）
  4. Checkpoint C2/C3 硬约束（不回复就等待，禁止跳过）

P1（提升质量到生产级）：
  5. 启发式原则库扩充（B端/数据产品特有问题模式）
  6. heuristic-engine 逻辑写入知识库/prompt（交叉判断+二次确认+去重）
  7. PRD-截图冲突分析环节（新增 stage 或合并到 Stage 5b）
  8. 输出分层（系统性归并 → 详细清单 → 证据包）
  9. golden sample 引入 prompt 作为 few-shot
  10. 任务清单"体验导向 vs 功能检查"判断标准强化

P2（长期改进）：
  11. 产品类型适配（B端/C端/工具/平台不同评估重点）
  12. 进化飞轮落地（trace + eval + 版本对比）
  13. 截图命名规范（输入质量标准）
```
