"""DesignOS Skills Factory package marker.

Per CONTRACT §6:
    - Factory may import from `kernel.*` only.
    - Factory must not import from `skills.*` or `mcp_servers.*`.
    - All kernel schema reuse goes through `_kernel_bridge`.
"""
