# 体验任务清单（简洁执行版）

> 给中低阶设计师 / 自动化按顺序执行用。
> 跑数据：{{run_id}}
> 总任务数：{{total_tasks}}
> 预计执行时长：{{estimated_duration_minutes}} 分钟

---

## 执行说明

1. 按本清单顺序执行（task_id 已按依赖排序）
2. 每个任务记录：
   - 是否能完成（yes / no / partial）
   - 关键证据截图（按 must_check 对应的原则）
   - 主观体验评分（1-5）
3. 完成后，把记录提交给 UXEval Agent 做归因

---

## 任务清单

| # | ID | 任务 | 角色 | 步骤摘要 | 必查原则 |
|---|---|---|---|---|---|
{{#each task_checklist_lite}}
| {{@index}} | {{id}} | {{title}} | {{role}} | {{steps_summary}} | {{must_check_inline}} |
{{/each}}

---

## 详细执行（每条复制即可）

{{#each task_checklist_lite}}

### {{id}} {{title}}

- **角色**：{{role}}
- **步骤**：{{steps_summary}}
- **必查原则**：
{{#each must_check}}
  - {{this}}
{{/each}}

执行记录：
- [ ] 完成情况：____ (yes / no / partial)
- [ ] 关键证据：____ (截图文件名)
- [ ] 体验评分：____ / 5
- [ ] 备注：____

---

{{/each}}

## 完成后提交

把执行记录文件 `任务执行记录.md` 放到 `runs/{{run_id}}/inputs/` 下，
然后跑：

```bash
designos run uxeval --resume --from heuristic-detection
```

UXEval Agent 会基于你的记录 + 截图证据继续归因。

> 由 UXEval Skill v0.2.0 生成
