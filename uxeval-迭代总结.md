# uxeval Skill 迭代总结报告

> **迭代周期**：2026-05-20（单日完成）
> **迭代版本**：v1.0.0 → v1.1.0（系统性升级）
> **总改动**：6 个 commit，16+ 个文件

---

## 一、迭代背景

### 起因

通过端到端测试（数据挖掘 V3.9.4 产品）发现 uxeval Skill 产出质量远低于 Codex（生产级标准）：

| 维度 | Codex | uxeval Skill（迭代前） |
|---|---|---|
| 问题数量 | 31 个 | 18 个 |
| Critical 问题 | 2 个（主路径异常） | 0 个 |
| 高优先级问题 | 19 个 | 2 个 |
| [inferred] 比例 | 低 | 高（~60%） |

### 核心问题诊断

经过系统性分析，识别出 **5 类系统性问题**：

1. **原则 ID 未更新**：30 个问题全部使用过时的 H 系列（H1-H12）
2. **场景-证据不匹配**：I-002 把空间详情页当作数据源下拉的证据（误判）
3. **遗漏主路径异常**：客户端模式无法发现 Codex 发现的 2 个 Critical 问题
4. **缺少系统性归纳**：仅按模块分布统计，缺乏业务视角
5. **截图识别准确性**：时间戳命名导致引用困难

### 终极目标

> 让 DesignOS 所有 Skills 产出**生产级可用**，甚至比中高级设计师更牛逼、更高效。
> 解放设计师，不需要设计师投入低阶工作。

---

## 二、迭代内容（13 项改动）

### 第一轮：8 项基础升级（commit 22e6345）

| # | 改动 | 文件 |
|---|---|---|
| 1 | SKILL.md 重写——强制 PDF 预处理 + 截图全量分析 | `SKILL.md` + `pipeline.yaml` |
| 2 | 启发式原则库重构——三层体系（P/F/S） | `reference/m02-启发式原则.md` |
| 3 | 问题检测逻辑强化——6 步流程 | `prompts/v1.0.0/06-issue-attribution.md` |
| 4 | 任务生成强化——必须基于完整旅程地图派生 | `prompts/v1.0.0/04-task-generation.md` + `reference/m04-任务生成.md` |
| 5 | PRD-截图冲突分析机制——新增 Stage 5.5 | `SKILL.md` + `pipeline.yaml` + 新增 `05c-conflict-analysis.md` |
| 6 | Golden Sample 引入 prompt 作为 few-shot | `prompts/v1.0.0/06-issue-attribution.md` |
| 7 | 宪法自检执行机制（Stage 6 输出前必须执行 7 步检查） | `constitution.md` |
| 8 | 输出模板升级——三层结构 + 4 级评分制 | `templates/问题报告.md` |

### 修正 1：截图分析 + 原则 ID（commit 3340015）

- SKILL.md：截图分析从"每批 5 张"改为"**每次只读 1 张**"（避免高分辨率截图爆 context）
- 02-principle-mapping.md：原则 ID 从 H 系列改为三层体系（P1-P4/F1-F8/S1-S5）

### 修正 2：宪法第 8 条（commit 367c6f8）

- 新增宪法第 8 条：**证据截图必须与问题场景匹配**
- 防止 I-002 类误判（场景描述与证据截图不符）

### 第二轮：P0 修复（commit b844804）

| # | 修复项 | 影响范围 |
|---|---|---|
| P0-1 | 原则 ID 全面更新为 P/F/S 系列 | 14 个文件 |
| P0-2 | 增加场景-证据匹配校验（Step 2.5 + 检查 1.5） | 2 个文件 |

**P0-1 映射关系**：
- H1 系统状态可见性 → F2
- H2 系统与现实匹配 → P2
- H3 用户控制与自由 → S1
- H5 错误预防 → F1
- H6 识别优于回忆 → S2
- H7 灵活性与效率 → S3
- H11 数据可信性 → F5
- H12 视觉层级 → P3

### 第三轮：P1 修复（commit 46a743b）

| # | 修复项 | 影响范围 |
|---|---|---|
| P1-1 | 增加异常场景任务（5 种类型 + 3 个示例） | 2 个文件 |
| P1-2 | 强化系统性归纳（业务视角 + 量化影响） | 1 个文件 |

**P1-1 异常场景类型**：
1. 上下文切换异常（多工作空间/多账号/多角色）
2. 页面渲染异常（长列表/大数据量/复杂图表）
3. 网络异常（超时/断网/同步失败）
4. 并发冲突（多人编辑/版本冲突/锁定）
5. 边界条件（空数据/超长输入/特殊字符）

### 第四轮：P2 修复（commit d07e8ac）

| # | 修复项 | 影响范围 |
|---|---|---|
| P2 | 改进截图识别准确性（content_description + evidence_content） | 2 个文件 |

---

## 三、修改文件清单（按目录分组）

### 顶层文档（2 个）
- `skills/uxeval/SKILL.md` ✅ 修改（PDF 预处理 + 截图分析 + Stage 5.5）
- `skills/uxeval/pipeline.yaml` ✅ 修改（新增 Stage 5.5）
- `skills/uxeval/constitution.md` ✅ 修改（7→8 条宪法 + 自检执行）

### Prompt 文件（8 个，含 1 个新增）
- `prompts/v1.0.0/02-principle-mapping.md` ✅ 修改（原则 ID）
- `prompts/v1.0.0/04-task-generation.md` ✅ 修改（执行流程 + Step 2.5）
- `prompts/v1.0.0/05a-script-generation.md` ✅ 修改（原则 ID）
- `prompts/v1.0.0/05b-screenshot-analysis.md` ✅ 修改（content_description）
- `prompts/v1.0.0/05c-conflict-analysis.md` ✅ **新增**（Stage 5.5）
- `prompts/v1.0.0/06-issue-attribution.md` ✅ 修改（6 步流程 + Step 2.5 + Step 6 强化 + evidence_content + Golden Sample）

### Reference 文件（3 个）
- `reference/m02-启发式原则.md` ✅ 修改（三层体系）
- `reference/m04-任务生成.md` ✅ 修改（异常场景示例）
- `reference/m06-问题归因.md` ✅ 修改（原则 ID）

### Template 文件（1 个）
- `templates/问题报告.md` ✅ 修改（三层结构 + 4 级评分）

### Eval 文件（6 个）
- `eval/promptfoo.yaml` ✅ 修改（原则 ID）
- `eval/golden/case-001-分类分级/annotations.yaml` ✅ 修改
- `eval/golden/case-001-分类分级/expected/04-任务清单-节选.md` ✅ 修改
- `eval/golden/case-001-分类分级/expected/06-问题清单.md` ✅ 修改
- `eval/golden/case-001-分类分级/input/raw_issues.json` ✅ 修改
- `eval/failure/F001-功能测试偏移/raw_issues_with_function_bugs.json` ✅ 修改

**合计**：20 个文件改动 + 1 个文件新增

---

## 四、Git 提交历史

```
d07e8ac feat(uxeval): P2修复完成 - 改进截图识别准确性
46a743b feat(uxeval): P1修复完成 - 异常场景任务 + 系统性归纳强化
b844804 fix(uxeval): P0修复完成 - 原则ID更新 + 场景证据匹配校验
367c6f8 feat(uxeval): 新增宪法第8条——证据截图必须与问题场景匹配
3340015 fix(uxeval): 截图分析改为逐张读取 + 原则ID统一为P/F/S系列
22e6345 feat(uxeval): 8项迭代升级——生产级质量提升
```

**所有 commit 已推送到 `origin/main`**。

---

## 五、预期效果（基于迭代方案）

### 关键指标改善预期

| 指标 | 迭代前（v1） | 迭代后（v2 预期） | Codex（标杆） |
|---|---|---|---|
| 问题总数 | 18 个（首次）→ 30 个（v1） | **25-35 个** | 31 个 |
| 原则 ID 体系 | H 系列（错误） | **P/F/S 三层（正确）** | 自定义 |
| Critical 问题 | 0 个 | **1-2 个**（异常场景任务支持） | 2 个 |
| 高优先级问题 | 2 个 | **15-20 个** | 19 个 |
| [inferred] 比例 | ~60% | **<30%** | 极低 |
| 系统性归纳 | 仅模块分布 | **5-10 类业务视角** | 5 类 |
| 异常场景覆盖 | 无 | **≥3 个任务** | 部分 |
| 场景-证据匹配 | 1 个误判（I-002） | **0 个误判** | 100% 匹配 |

### 质量提升预期

1. **PRD 理解**：从泛化到精准（强制 pdftotext，禁止多模态"看"PDF）
2. **截图分析**：从部分到全量（逐张读取，禁止跳过）
3. **原则应用**：从过时（H 系列）到现代（P/F/S 三层）
4. **问题质量**：从推断到证据（场景-证据匹配 + 宪法自检）
5. **报告结构**：从扁平到分层（执行摘要 + 系统性归纳 + 详细清单）

---

## 六、未解决的问题

### 1. 客户端模式的固有局限

**问题**：截图无法验证交互行为，无法发现主路径异常

**影响**：可能仍会遗漏部分 Critical 问题

**当前缓解**：
- ✅ 增加异常场景任务（用户可手动执行）
- ✅ 增加 PRD-截图冲突分析
- ⚠️ 真正解决需要 Web 模式（M2 阶段）

### 2. AI 截图识别准确性

**问题**：高分辨率截图可能识别错误（如把空间详情页认作数据源下拉）

**当前缓解**：
- ✅ 截图分析输出 content_description
- ✅ 问题检测校验 evidence_content
- ✅ 宪法第 8 条强制场景-证据匹配

**残余风险**：如果 content_description 本身识别错了，校验会失效

### 3. 第三次端到端测试未执行

**问题**：所有修复都基于代码审查，未通过实际跑一次验证

**建议**：第二天用 designos1 输入重新跑一次完整 pipeline

---

## 七、后续建议

### 立即行动（明日）

1. **第三次端到端测试**：用 designos1 输入重新跑，验证所有修复生效
2. **逐条审查问题清单**：检查是否还有 I-002 类误判
3. **对比三方产出**：Codex / v1 / v2，确认改善幅度

### 短期（本周）

1. **增加自动化测试**：把 7 项集成测试做成 CI 脚本
2. **完善 evaluation harness**：用 eval/golden 做回归测试
3. **更新 CHANGELOG**：记录 v1.1.0 的所有改动

### 中期（M2 阶段）

1. **增强 Web 模式**：用 Playwright 自动验证主路径异常
2. **AI 多 reviewer 模式**：用多个模型交叉验证问题质量
3. **真实环境集成**：与 design-acceptance Skill 联动

---

## 八、关键决策记录

### 决策 1：从"每批 5 张"改为"每次 1 张"

**问题**：高分辨率截图导致 context 爆掉
**决策**：截图分析改为逐张读取
**代价**：执行时间变长（26 张 × 10s = 260s）
**收益**：覆盖率从部分到全量

### 决策 2：从"H 系列"改为"P/F/S 三层"

**问题**：H1-H12 与现代用户体验设计原则脱节
**决策**：采用三层体系（表现层 P / 框架层 F / 结构层 S）
**代价**：需要更新 14 个文件的示例
**收益**：原则与现代设计语言对齐

### 决策 3：增加宪法第 8 条

**问题**：场景-证据不匹配导致误判
**决策**：增加宪法第 8 条 + 检查 1.5
**代价**：可能导致问题数量减少
**收益**：避免 I-002 类误判

### 决策 4：保留客户端模式 + 增加异常场景任务

**问题**：客户端模式无法发现主路径异常
**决策**：保留客户端模式（成本可控），但增加异常场景任务（用户手动执行）
**代价**：无法 100% 自动化
**收益**：在客户端模式下也能识别 Critical 问题（通过用户提供的异常场景截图）

---

**报告生成时间**：2026-05-20 23:35
**自动化执行人**：Claude Opus 4.7
**Git 远程状态**：所有 commit 已推送
