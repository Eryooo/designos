# 体验导向任务清单（完整版）

> 给资深设计师做整体把控用。
> 跑数据：{{run_id}}
> 模式：{{mode}}
> 总任务数：{{total_tasks}}

---

## 任务覆盖度概览

| 维度 | 数量 | 备注 |
|---|---|---|
| 总任务 | {{total_tasks}} | |
| 涉及模块 | {{module_coverage}} / {{total_modules}} | 每模块至少 2 个 |
| 涉及旅程阶段 | {{stage_coverage}} / {{total_stages}} | 每阶段 2-5 个 |
| KeyTask 覆盖 | {{key_task_coverage}} / {{total_key_tasks}} | 每 KT 至少 3 个 |
| 应用原则 | {{principle_coverage}} 条 | 总原则 {{total_principles}} 条 |

---

## 任务清单

{{#each task_checklist_full}}

### {{id}} {{title}}

| 字段 | 值 |
|---|---|
| 角色 | {{role}} |
| 场景 | {{scenario}} |
| 旅程阶段 | {{journey_stage_id}} {{journey_stage_name}} |
| 适用原则 | {{applicable_principles_with_names}} |

**任务描述**：

{{description}}

**前置条件**：
{{#each prerequisites}}
- {{this}}
{{/each}}

**执行步骤**：
{{#each steps}}
{{this}}
{{/each}}

**验收标准（success_criteria）**：
{{#each success_criteria}}
- [ ] {{this}}
{{/each}}

**证据要求**：
{{#each evidence_requirements}}
- {{this}}
{{/each}}

---

{{/each}}

## 任务索引（按模块）

{{#each tasks_by_module}}
### {{module_name}}（{{module_id}}）
{{#each tasks}}
- {{id}} {{title}}
{{/each}}
{{/each}}

## 任务索引（按旅程阶段）

{{#each tasks_by_stage}}
### {{stage_name}}（{{stage_id}}）
{{#each tasks}}
- {{id}} {{title}}
{{/each}}
{{/each}}

---

## 备注

- 完整版任务用于资深设计师把控覆盖度，简洁版（`03-任务清单-简洁版.md`）用于中低阶设计师顺序执行
- 在 Checkpoint C2 阶段，请重点检查：
  1. 是否偏功能测试（每个任务必须有 applicable_principles）
  2. 是否漏掉关键体验任务（特别是 critical_pain 阶段）
  3. 任务粒度是否合理（不要过粗也不要过细）

> 由 UXEval Skill v0.2.0 生成
