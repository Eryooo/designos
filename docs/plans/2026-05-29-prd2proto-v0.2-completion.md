# prd2proto v0.2 完成存档（2026-05-29）

> 状态：v0.2 三个并行 Agent 全部完成，300/300 测试零回归，等用户跑真 PRD 验证
> 上游：v0.1 完成存档见 `2026-05-29-prd2proto-p1-completion.md`
> 试跑手册：`2026-05-29-prd2proto-p1-trial-runbook.md`（v0.1 写的，v0.2 通用）

---

## 1. v0.2 触发原因

用户用 Claude Code + Trae 两个 LLM 端各跑了一次真 PRD（小飞棍任务平台），写了两份吐槽报告：

- `/Users/young/Documents/claude-code/designos/prd2proto-run/REPORT-skill-iteration.md`
- `/Users/young/Documents/trae_projects/xfg-ui/prd2proto_skill_report.md`

**核心痛点**：用户多次骂"卡半天没反应"，最终代码能跑但体验差。

---

## 2. v0.2 改了什么（简表）

### 新增

| 路径 | 内容 |
|---|---|
| `skills/prd2proto/reference/design-templates/` | 8 个 Google Labs design.md 格式预置模板（stripe/linear/vercel/notion/coze/antd-pro/apple-hig/arco）+ README 含 5 维选择算法 |
| `skills/prd2proto/prompts/04-token-extraction.md` | W3C DTCG 格式硬约束 prompt |
| `skills/prd2proto/prompts/05-code-generation.md` | 代码生成 prompt：钉死项目骨架 / 状态管理 / mock 规模 |
| `skills/prd2proto/prompts/07-liveness-check.md` | 引导用户跑 npm install/dev 并回报 URL |
| `skills/prd2proto/reference/m04-token-extraction.md` | DTCG checklist |
| `skills/prd2proto/reference/m05-code-generation.md` | 代码生成 checklist |
| `skills/prd2proto/reference/textbook/` | 教学材料移到子目录（atomic-design / story-mapping / design-tokens-w3c / code-constitution-rationale），LLM 默认不读 |
| `skills/prd2proto/tests/test_design_templates.py` | 34 个模板格式测试 |
| `skills/prd2proto/tests/test_reference_consistency.py` | 10 个 reference 行数 / 引用一致性测试 |

### 重写

| 路径 | 关键变化 |
|---|---|
| `skills/prd2proto/SKILL.md` | 加 **Progress Contract**（60s 沉默上限）/ **Checkpoint Behavior**（聊天模式 C1/C2/C3 默认 continue）/ Loading Discipline / Output Path Convention / 实装现状表；删除 Step 0 冗余 boilerplate；版本 0.1.0→0.2.0 |
| `skills/prd2proto/pipeline.yaml` | 8 stages：token-extraction 和 code-generation 改 type:llm（不再是 mock tool）；新增 liveness-check（type:llm）；删 component-mapping；C1-C4 message 改简短风格 |
| `skills/prd2proto/constitution.md` | 替换 uxeval 占位为真正 4 条代码宪法 + 白名单 + AntD theme.ts 例外 |
| `skills/prd2proto/prompts/03a-spec-generation.md` | 改为"从模板库选 + 微调"5 步算法，输出 `selected_template` / `selection_reason` / `adjustments` 字段 |
| `skills/prd2proto/reference/m01..m06` | 全部从教学材料浓缩为 ≤100 行 checklist，平均缩 60% |
| `skills/prd2proto/tests/test_p1_smoke.py` | 14 tests（含 4 个 v0.2 契约新增） |

---

## 3. 测试基线（v0.2 完成时）

| 套件 | 通过 |
|---|---:|
| Kernel 单元测试 | 180/180 |
| 工厂回归 | 46/46 |
| frontend-codegen Mock | 12/12 |
| prd2proto P1 smoke | 14/14 |
| prd2proto design-templates | 34/34 |
| prd2proto reference consistency | 10/10 |
| prd2proto frontmatter / integration | 4/4 |
| **合计** | **300/300** |

零 kernel/工厂回归。`python3 -m tools.validate ../skills/prd2proto --archetype generation` 通过。

---

## 4. 下次接续要做的事

### 4.1 等用户跑真 PRD 验证

按 `docs/plans/2026-05-29-prd2proto-p1-trial-runbook.md` 跑。建议另开一个工作目录，不要在开发目录里跑（避免污染）。

### 4.2 用户反馈写到

`docs/plans/2026-05-29-prd2proto-v0.2-trial-feedback.md`（用户填）

### 4.3 提交 + 推送（v0.2 这一轮还没 commit）

工作区状态（截至存档时）：
```
modified:
  M skills/prd2proto/SKILL.md
  M skills/prd2proto/constitution.md
  M skills/prd2proto/pipeline.yaml
  M skills/prd2proto/prompts/03a-spec-generation.md
  M skills/prd2proto/reference/m01..m06 (4 个浓缩)
  M skills/prd2proto/tests/test_p1_smoke.py

untracked:
  ?? skills/prd2proto/prompts/{04,05,07}-*.md
  ?? skills/prd2proto/reference/design-templates/
  ?? skills/prd2proto/reference/m04..m05
  ?? skills/prd2proto/reference/textbook/
  ?? skills/prd2proto/tests/test_{design_templates,reference_consistency}.py

(忽略 .claude/settings.local.json 和 designos/__init__.py 的本地变化)
```

**建议 commit 拆分**：
- 1 个 commit：v0.2 prd2proto 改造（含三个 Agent 全部产出）
- commit message 范例见下文

**当前分支**: `factory-and-prd2proto-p1`（已推 origin）

---

## 5. v0.2 commit message 草稿（备用）

```
feat(prd2proto): v0.2 — fix UX freezes + add design.md templates + reach 300/300 tests

Triggered by two real-PRD trial reports (Claude Code + Trae) where the user
repeatedly complained "卡半天没反应". Final code worked, but UX failed.

Three parallel agents shipped:

Agent A — design.md template library (skills/prd2proto/reference/design-templates/):
- 8 Google-Labs-format DESIGN.md presets: stripe, linear, vercel, notion,
  coze, antd-pro, apple-hig, arco. Each ~119 lines, YAML frontmatter +
  Markdown body.
- README with 5-dimension matching algorithm + adjustment whitelist.
- 34 template format tests.

Agent B — SKILL.md + pipeline.yaml v0.2:
- SKILL.md adds Progress Contract (60s silence cap, 6 mandatory rules),
  Checkpoint Behavior (chat mode defaults to continue at C1/C2/C3),
  Loading Discipline, Output Path Convention, "implementation status"
  table. Deletes Step 0 boilerplate.
- pipeline.yaml: 8 stages. token-extraction and code-generation move from
  type: tool (mock MCP) to type: llm with real prompts. component-mapping
  removed (deferred). liveness-check added (type: llm guides user to run
  npm install/dev).
- prompts/04-token-extraction.md (new), prompts/05-code-generation.md (new),
  prompts/07-liveness-check.md (new).
- test_p1_smoke.py: 14 tests, 4 of which lock the new v0.2 contracts.

Agent C — 03a rewrite + reference compression:
- prompts/03a-spec-generation.md: from "generate from scratch" to
  "select from template library + adjust" (5-step algorithm). Output
  schema gains selected_template / selection_reason / adjustments.
- All reference/m*.md compressed by 48-69% (max 100 lines each), with
  textbook material relocated to reference/textbook/.
- constitution.md replaces uxeval placeholder with real 4-rule code
  constitution + AntD theme.ts exception + border-width whitelist.

Test baseline at v0.2:
- kernel/tests/unit: 180/180
- .factory/tests: 46/46
- mcp-servers/frontend-codegen/tests: 12/12
- skills/prd2proto/tests: 62/62 (was 14)
- Total: 300/300, zero regression vs v0.1.

User feedback addressed:
- "你在干啥半天没反应" → Progress Contract enforces output every <=60s
- "效果不好风格很丑" → 8 design.md presets + AI auto-match by product fit
- "design.md 应该可选" → 3-tier strategy (user provides / template / re-pick)
- "design.md 应该参考 Google Stitch + GitHub" → format follows
  google-labs-code/design.md spec; templates inspired by VoltAgent/awesome-design-md.
- "把导航路由都加上" → prompts/05 nails project skeleton (router + Shell + real links).

Skill version bumped 0.1.0 -> 0.2.0 in SKILL.md frontmatter.
designos package version (pyproject.toml) unchanged at 0.5.0a1; will bump
to 0.5.0a2 only after real-PRD trial validates v0.2.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

---

## 6. 下次开会话第一件事读这些

按顺序读：
1. 本文件（v0.2 整体进度）
2. `docs/plans/2026-05-29-prd2proto-p1-completion.md`（v0.1 完成报告）
3. `docs/plans/2026-05-29-prd2proto-implementation-plan.md`（P1-P4 路线）
4. `skills/prd2proto/SKILL.md`（最新 skill 真源）
5. `skills/prd2proto/pipeline.yaml`
6. memory 索引：`/Users/young/.claude/projects/-Users-young-Documents-Codex-Agent-design-webmode/memory/MEMORY.md`

如有 v0.2 试跑反馈：
7. `docs/plans/2026-05-29-prd2proto-v0.2-trial-feedback.md`（用户填，可能不存在）

---

## 7. 关键不要忘的事

1. **当前 prd2proto 是手工版**：frontend-codegen 是 Mock，stage 4/5/6/7 全靠 LLM 写。这是有意为之，等 P3 真 MCP 实装后改回 type:tool。
2. **modes 不能跨 archetype 强行统一**：evaluation 的 mode = evidence_collection，generation 的 mode = fidelity，analysis 的 mode = data_source。这写在工厂 archetype.yaml 里。
3. **design-spec.md 不是必填项**：v0.1 是必填，v0.2 改成可选 + AI 自动从模板库选。
4. **v0.2 没有 commit/push**：30+ 文件都在工作区，等用户决定何时 commit。
5. **/prd2proto 软链已建**：另一个 CC 窗口能识别 skill。改 skill 文件后**必须重启**那个窗口才生效。
6. **不要在开发目录跑真 PRD**：会污染（memory 写入 / outputs/ / inputs/）。建议另外一个 trial 目录跑。

---

## 8. P3+ 路线快速回顾

下一阶段（不是这次的事）：
- **P3 frontend-codegen 真 MCP 实装**（最重要瓶颈，预计 5-10 工作日）：DSL 适配层（Figma + MasterGo）+ Token 提取器 + 组件映射器 + 真代码生成器 + 4 条宪法的静态扫描器
- **P4 designer-dsl 模式真实跑通**（接 P3）
- **P5 IDE slash 深度整合**（弹 UI / 真 orchestrator）
- **P6 跨 skill 协同**（接 ai-analytics 上游 / 输出给 design-acceptance）

详见 `docs/plans/2026-05-29-prd2proto-implementation-plan.md`。

---

## 9. 一句话存档

> v0.2 把 v0.1 的"代码能跑但 UX 烂"治好了，但还没在真 PRD 上验证；30+ 文件在工作区未 commit；下次回来先读本文件 + 等用户试跑反馈，再决定是 commit 还是继续改。
